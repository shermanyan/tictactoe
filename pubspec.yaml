name: tictactoe_flutter
description: "tic tac toe."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.5
  hydrated_bloc: ^10.0.0
  http: ^1.3.0
  web_socket_channel: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
